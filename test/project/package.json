{
  "private": true,
  "devDependencies": {
    "del-cli": "^4.0.1",
    "parcel": "^2.0.0",
    "parcel-transformer-graphviz": "file:../../"
  },
  "scripts": {
    "build": "parcel build --cache-dir .parcel-cache --no-cache --no-optimize test.gv",
    "clean": "del .parcel-cache dist"
  }
}
