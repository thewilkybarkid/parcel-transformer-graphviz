{
  "private": true,
  "dependencies": {
    "del-cli": "^4.0.1",
    "parcel": "^2.0.0-rc.0",
    "parcel-transformer-graphviz": "file:../../"
  },
  "scripts": {
    "build": "parcel build --no-cache --no-optimize test.gv",
    "clean": "del .parcel-cache dist"
  }
}
